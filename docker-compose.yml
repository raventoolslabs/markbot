# Redes: 'raventoolslabs' une markbot con Pergamo; 'db' y 'redis' dan acceso al
# postgres y al Redis compartidos (solo el backend); 'proxy' es por donde entra
# el nginx del host.
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: markbot-backend
    ports:
      - "3002:3002"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - PORT=3002
      - NODE_ENV=production
      # Redis compartido: la cola usa bull:token-refresh, el mismo que markbot en
      # desarrollo, así que no deben correr los dos a la vez contra él.
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST:-postgres}:5432/${POSTGRES_DB}?schema=markbot
      - CHAT_PROVIDER
      - CHAT_API_KEY
      - CHAT_MODEL_NAME
      - CHAT_TEMPERATURE
      - OLLAMA_BASE_URL
      # Dentro de la red raventoolslabs Pergamo siempre está en su nombre de servicio.
      - PERGAMO_URL=http://pergamo:3000
      - PERGAMO_ORGANIZATION
      - PERGAMO_PASSWORD
      - GOOGLE_CLIENT_ID
      - GOOGLE_CLIENT_SECRET
      - GOOGLE_CLIENT_SCOPE
      - SMTP_HOST
      - SMTP_PORT
      - SMTP_USER
      - SMTP_PASSWORD
    networks:
      - raventoolslabs
      - db
      - redis
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: markbot-web
    # 3003 en el host: el 3000 es de Pergamo.
    ports:
      - "3003:3000"
    environment:
      - API_URL=http://backend:3002
    depends_on:
      - backend
    # 'proxy': el nginx del host la publica por su nombre.
    networks:
      - raventoolslabs
      - proxy
    restart: unless-stopped

networks:
  raventoolslabs:
    external: true
  db:
    external: true
  redis:
    external: true
  proxy:
    external: true
